package dia2;

import java.util.Scanner;

public class Ejer7 {

	/**
	 * Ejercicio Nº 7 - Arreglos: Vectores Realizar lo mismo que en el ejercicio 4,
	 * pero utilizando un vector para guardar los números ingresados. Una vez
	 * almacenados los números, deberá recorrer el vector, realizar la suma y luego
	 * mostrar por pantalla.
	 * 
	 * Ejercicio Nº 4 - Estructuras Repetitivas Crear un programa en Java que
	 * permita ingresar 8 números (valor entero). Utilizar la estructura repetitiva
	 * “while” Luego debe mostrar la suma de los números ingresados.
	 * 
	 * @param args
	 */
	public static void main(String[] args) {
		int i;
		try (Scanner scanner = new Scanner(System.in)) {
			int[] numeros = {};  
			while ((i = 0) <= 7) {
				i++;
				scanner.nextInt();
			}
		}
	}

}
