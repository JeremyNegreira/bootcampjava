package discoDuroDeRoerEjerciciosBasicosJavaPropuestos;

public class Ejer2 {

	/**
	 * 2) Declara 2 variables numéricas (con el valor que desees), e indica cual es
	 * mayor de los dos. Si son iguales indicarlo también. Ves cambiando los valores
	 * para comprobar que funciona.
	 * 
	 * @param args
	 */
	public static void main(String[] args) {
		int numA1 = 0, numA2= 1;
		System.out.println(numA1 != numA2 ? Math.max(numA1, numA2) + " es mayor que " + Math.min(numA1, numA2) : "Son iguales");
		int numB1 = 1;
		System.out.println(numB1 != numA2 ? Math.max(numB1, numA2) + " es mayor que " + Math.min(numB1, numA2) : "Son iguales");
		int numC1 = 2;
		System.out.println(numC1 != numA2 ? Math.max(numC1, numA2) + " es mayor que " + Math.min(numC1, numA2) : "Son iguales");
	}

}
